import { ReportStatisticsRequest } from './report-statistics-request';

describe('ReportStatisticsRequest', () => {
  it('should create an instance', () => {
    expect(new ReportStatisticsRequest()).toBeTruthy();
  });
});
