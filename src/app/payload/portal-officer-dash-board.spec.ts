import { PortalOfficerDashBoard } from './portal-officer-dash-board';

describe('PortalOfficerDashBoard', () => {
  it('should create an instance', () => {
    expect(new PortalOfficerDashBoard()).toBeTruthy();
  });
});
