import { VerificationRequestDetails } from './verification-request-details';

describe('VerificationRequestDetails', () => {
  it('should create an instance', () => {
    expect(new VerificationRequestDetails()).toBeTruthy();
  });
});
