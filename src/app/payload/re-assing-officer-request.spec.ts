import { ReAssingOfficerRequest } from './re-assing-officer-request';

describe('ReAssingOfficerRequest', () => {
  it('should create an instance', () => {
    expect(new ReAssingOfficerRequest()).toBeTruthy();
  });
});
