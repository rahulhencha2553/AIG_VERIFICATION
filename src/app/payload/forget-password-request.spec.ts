import { ForgetPasswordRequest } from './forget-password-request';

describe('ForgetPasswordRequest', () => {
  it('should create an instance', () => {
    expect(new ForgetPasswordRequest()).toBeTruthy();
  });
});
