import { OfficerUpdateStatus } from './officer-update-status';

describe('OfficerUpdateStatus', () => {
  it('should create an instance', () => {
    expect(new OfficerUpdateStatus()).toBeTruthy();
  });
});
