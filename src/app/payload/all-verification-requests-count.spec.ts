import { AllVerificationRequestsCount } from './all-verification-requests-count';

describe('AllVerificationRequestsCount', () => {
  it('should create an instance', () => {
    expect(new AllVerificationRequestsCount()).toBeTruthy();
  });
});
