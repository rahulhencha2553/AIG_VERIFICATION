import { UpdateOfficerRequest } from './update-officer-request';

describe('UpdateOfficerRequest', () => {
  it('should create an instance', () => {
    expect(new UpdateOfficerRequest()).toBeTruthy();
  });
});
