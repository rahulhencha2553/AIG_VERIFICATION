import { OfficerAssignedRequests } from './officer-assigned-requests';

describe('OfficerAssignedRequests', () => {
  it('should create an instance', () => {
    expect(new OfficerAssignedRequests()).toBeTruthy();
  });
});
