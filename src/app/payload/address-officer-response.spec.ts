import { AddressOfficerResponse } from './address-officer-response';

describe('AddressOfficerResponse', () => {
  it('should create an instance', () => {
    expect(new AddressOfficerResponse()).toBeTruthy();
  });
});
